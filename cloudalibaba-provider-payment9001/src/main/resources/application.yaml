server:
  port: 9001
spring:
  application:
    name: service-provider
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
#        暴露所有的端口用来作为监控入口
management:
  endpoints:
    web:
      exposure:
        include: '*'